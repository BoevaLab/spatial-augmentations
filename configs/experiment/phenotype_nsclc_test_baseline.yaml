# @package _global_

# to execute this experiment run:
# python src/eval_phenotype.py experiment=phenotype_nsclc_test_baseline

defaults:
  - override /data: cellular_graph
  - override /model: gnn_pred_phenotype
  - override /trainer: gpu

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

task_name: "phenotype_nsclc_test_baseline"
tags: ["phenotype", "nsclc", "test", "baseline"]
ckpt_path: ${paths.log_dir}phenotype_nsclc_hparam_baseline/multiruns/2025-05-17_16-11-54/7/checkpoints/epoch_epoch=3999.ckpt

data:
  mode: "evaluation"

model:
  mode: "evaluation"
  test_tresh: 0.5
